# SQL-Employee-Tracker

## Description

An SQL Database that tracks employees and their info including roles, department, and salary.

## Starting the application
Clone this repository then run the following command from the command line: `node employeeManager.js`

Then you will be prompted to answer questions about the staff personnel you are adding to the database. Upon answering all questions, your database will be populated with new employees and their information.